<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- FOREGROUND_SERVICE_MICROPHONE declared but only used when mic is active -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- v3.1.90: declare TTS engine intent so Android 11+
         package visibility allows us to discover installed
         TTS engines. Without this, TextToSpeech onInit
         returns status=-1 (ERROR) on devices where the
         TTS engine is installed but only resolvable via
         Intent. The Google TTS engine is the most common
         affected one — it's a separate APK (com.google.android.tts)
         that needs to be discovered across the package
         visibility boundary. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
        <!-- v3.1.90: also declare the install intent so we
             can prompt the user to install a TTS engine if
             none is available. The standard TTS install
             intent opens Play Store to the user's default
             TTS engine picker. -->
        <intent>
            <action android:name="android.intent.action.INSTALL_TTS_DATA" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:supportsRtl="true">

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:showWhenLocked="true"
        android:turnScreenOn="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!-- Background service for persistent notification -->
      <service
        android:name=".CyberClawService"
        android:foregroundServiceType="microphone"
        android:stopWithTask="true"
        android:exported="false" />

      <receiver
        android:name=".WakeReceiver"
        android:exported="false">
        <intent-filter>
          <action android:name="com.cyberclawmobile.ACTION_WAKE" />
        </intent-filter>
      </receiver>


    </application>
</manifest>

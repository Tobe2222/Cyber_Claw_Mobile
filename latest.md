## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 14:51 (channel: #cyber-dev)
- **User:** Tobe reported "actually i would like to introduce the food/treats on the mobile end also. Put that in the lower left corner. Just like it is on desktop."
- **You:** v3.10.72 ships the food/treats feature on mobile, mirroring the desktop's arena feed button + treats system. (1) Mobile arena treats system ported from desktop's pixel-arena.js: treats array with 60s lifetime + 500ms grace period, dropTreat() API places at canvas center (mobile has no drag-to-place, tap = drop), seek-and-eat logic in update() with the active companion walking toward treats <30px and playing 😋+❤️ emoji overlay, companionEmoji DOM overlay positioned above the companion's head. (2) Bottom-left arena button (`#ctrlBottomLeft`) matching desktop's #arena-feed-btn CSS, posts {type:'feed'} to RN. (3) React Native bottom-sheet Modal with 7 treats (apple/burger/meat/fish/cake/cookie/berries) styled like desktop's #feed-menu. Tap → `injectJavaScript('window.Arena.dropTreat("apple")')`. (4) End-to-end IPC: mobile → WebView → RN → sync-server → desktop renderer → promptCompanionReaction('I just gave you X. What do you think?'). The desktop side ships as v3.2.15 (companion release). Bumped to v3.10.72 (versionCode 298). Pushed branch + tag to origin. See CHANGES_3.10.72.md for the breakdown and the lessons "tap-to-place is the natural mobile flow" + "mobile-initiated actions should reuse the desktop's existing AI hooks unchanged". Toys (⚽⚾🧶) intentionally out of scope for now — they need physics which the mobile arena doesn't support.

## 2026-07-22 14:51 (channel: #cyber-dev)
- **User:** Tobe reported "actually i would like to introduce the food/treats on the mobile end also. Put that in the lower left corner. Just like it is on desktop."
- **You:** v3.10.72 ships the food/treats feature on mobile, mirroring the desktop's arena feed button + treats system. (1) Mobile arena treats system ported from desktop's pixel-arena.js: treats array with 60s lifetime + 500ms grace period, dropTreat() API places at canvas center (mobile has no drag-to-place, tap = drop), seek-and-eat logic in update() with the active companion walking toward treats <30px and playing 😋+❤️ emoji overlay, companionEmoji DOM overlay positioned above the companion's head. (2) Bottom-left arena button (`#ctrlBottomLeft`) matching desktop's #arena-feed-btn CSS, posts {type:'feed'} to RN. (3) React Native bottom-sheet Modal with 7 treats (apple/burger/meat/fish/cake/cookie/berries) styled like desktop's #feed-menu. Tap → `injectJavaScript('window.Arena.dropTreat("apple")')`. (4) End-to-end IPC: mobile → WebView → RN → sync-server → desktop renderer → promptCompanionReaction('I just gave you X. What do you think?'). The desktop side ships as v3.2.15 (companion release). Bumped to v3.10.72 (versionCode 298). Pushed branch + tag to origin. See CHANGES_3.10.72.md for the breakdown and the lessons "tap-to-place is the natural mobile flow" + "mobile-initiated actions should reuse the desktop's existing AI hooks unchanged". Toys (⚽⚾🧶) intentionally out of scope for now — they need physics which the mobile arena doesn't support.


## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 14:31 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu and we can remove the extra clawsuu is thinking text in the arena, it is already in the chat as you can see" — screenshot showed the same "thinking" status in two places: orange bar above the chat tabs ("💭 Clawsuu is thinking..." hard-coded name) AND below the chat above the input ("<name> is thinking..." via chatVoiceStatus, correct name).
- **You:** v3.10.69 removes the redundant React `<View>` bar and its associated `thinkingBar` / `thinkingText` styles. The chat-side `chatVoiceStatus` indicator stays (it's the one Tobe wants). Kept `setArenaThinking()` which injects JS into the arena WebView to animate the companion sprite (the sprite still shows it's thinking, only the redundant text bar is gone). Left the dead `isThinking` state in place since removing it cleanly is one more line of churn for negligible gain. Bumped to v3.10.69 (versionCode 295). Pushed branch + tag to origin. See CHANGES_3.10.69.md for the breakdown and the lessons "don't show the same status in two places" + "the arena WebView sprite animation is independent of the React-side status text" + "hard-coded UI strings rot fast — Clawsuu was right when there was one companion".

## 2026-07-22 14:31 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu and we can remove the extra clawsuu is thinking text in the arena, it is already in the chat as you can see" — screenshot showed the same "thinking" status in two places: orange bar above the chat tabs ("💭 Clawsuu is thinking..." hard-coded name) AND below the chat above the input ("<name> is thinking..." via chatVoiceStatus, correct name).
- **You:** v3.10.69 removes the redundant React `<View>` bar and its associated `thinkingBar` / `thinkingText` styles. The chat-side `chatVoiceStatus` indicator stays (it's the one Tobe wants). Kept `setArenaThinking()` which injects JS into the arena WebView to animate the companion sprite (the sprite still shows it's thinking, only the redundant text bar is gone). Left the dead `isThinking` state in place since removing it cleanly is one more line of churn for negligible gain. Bumped to v3.10.69 (versionCode 295). Pushed branch + tag to origin. See CHANGES_3.10.69.md for the breakdown and the lessons "don't show the same status in two places" + "the arena WebView sprite animation is independent of the React-side status text" + "hard-coded UI strings rot fast — Clawsuu was right when there was one companion".


## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 14:41 (channel: #cyber-dev)
- **User:** Tobe reported two things: "i also see that there is a gap in the bottom, under the chat input field" (visible dark band between input row and bottom of screen) and "when the companion answers we should get a notification on the phone, there it can say which companion that has answered etc." (no system ping when a companion replies).
- **You:** v3.10.70 ships both. (1) Gap fix: imported `useSafeAreaInsets` from `react-native-safe-area-context` in HomeScreen and applied `paddingBottom: 8 + insets.bottom` to the `inputContainer`. The RN `SafeAreaView` is iOS-only — Android nav-bar inset (~48dp) was leaving empty space because the app's content didn't extend behind the system nav bar. On iOS with no home indicator, `insets.bottom === 0` and behavior is unchanged. (2) Notifications: new `notifyCompanionReply(agentName, text)` method on `NativeBackgroundModule` (Kotlin), uses a new `cyberclaw_chat` notification channel so the user can mute chat notifications independently from the BG-listening / wake channels. Notification IDs are scoped per companion (hash of name) so rapid replies from Clawsuu overwrite each other but Lamasuu keeps its own thread. Title is "Lamasuu replied", body is the reply text truncated to 140 chars. JS-side condition: fires when `isUser === false` AND user isn't actively watching that companion's chat (app backgrounded, different tab, voice mode, or different companion focused) — uses existing ref pattern (`appStateRef`, `fullscreenRef`, `isWakeWordModeRef`, plus new `activeTabRef`). Bumped to v3.10.70 (versionCode 296). Pushed branch + tag to origin, CI building now. See CHANGES_3.10.70.md for the breakdown and the lessons "the RN SafeAreaView is iOS-only — for Android nav-bar padding use useSafeAreaInsets" + "notifications are a UX feature, not just native plumbing — get the WHEN right before optimizing the channel or icon" + "per-resource notification IDs prevent spam without silencing cross-resource distinctions".

## 2026-07-22 14:41 (channel: #cyber-dev)
- **User:** Tobe reported two things: "i also see that there is a gap in the bottom, under the chat input field" (visible dark band between input row and bottom of screen) and "when the companion answers we should get a notification on the phone, there it can say which companion that has answered etc." (no system ping when a companion replies).
- **You:** v3.10.70 ships both. (1) Gap fix: imported `useSafeAreaInsets` from `react-native-safe-area-context` in HomeScreen and applied `paddingBottom: 8 + insets.bottom` to the `inputContainer`. The RN `SafeAreaView` is iOS-only — Android nav-bar inset (~48dp) was leaving empty space because the app's content didn't extend behind the system nav bar. On iOS with no home indicator, `insets.bottom === 0` and behavior is unchanged. (2) Notifications: new `notifyCompanionReply(agentName, text)` method on `NativeBackgroundModule` (Kotlin), uses a new `cyberclaw_chat` notification channel so the user can mute chat notifications independently from the BG-listening / wake channels. Notification IDs are scoped per companion (hash of name) so rapid replies from Clawsuu overwrite each other but Lamasuu keeps its own thread. Title is "Lamasuu replied", body is the reply text truncated to 140 chars. JS-side condition: fires when `isUser === false` AND user isn't actively watching that companion's chat (app backgrounded, different tab, voice mode, or different companion focused) — uses existing ref pattern (`appStateRef`, `fullscreenRef`, `isWakeWordModeRef`, plus new `activeTabRef`). Bumped to v3.10.70 (versionCode 296). Pushed branch + tag to origin, CI building now. See CHANGES_3.10.70.md for the breakdown and the lessons "the RN SafeAreaView is iOS-only — for Android nav-bar padding use useSafeAreaInsets" + "notifications are a UX feature, not just native plumbing — get the WHEN right before optimizing the channel or icon" + "per-resource notification IDs prevent spam without silencing cross-resource distinctions".


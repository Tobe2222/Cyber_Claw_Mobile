## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 17:31 (channel: #cyber-dev)
- **User:** Tobe reported 5 things from testing the snack + quest edit: (1) "the companion did not move to it" — snacks dropped but no seek behavior; (2) "it should be drag and drop able also" — wants drag-to-place; (3) "the comments the companion makes when given food or played with does not need to appear in the chat" — no AI reaction noise on feed/play; (4) "certain companions should prefer certain foods" — per-companion preferences; (5) quest edit still says "Couldn't update quest: quest not found" even with v3.10.73's gate, plus "it does not need the double active quest or not" (the Status="Active" + ⚡ ACTIVE badge confusion) and "still missing the steps" (goal text editing).
- **You:** v3.10.74 ships all five. (1) Companion-not-moving fix: removed `c.isActive` requirement from seek-and-eat — the v3.10.72 logic only ran for active companions but on the home screen the user can drop a treat before tapping a companion tab (activeId is null at that moment). Desktop doesn't filter by active either, mobile now matches. Added 200px L∞ distance threshold for sanity. (2) Drag-to-place: long-press a treat in the modal → drag overlay follows finger → release places at finger position via PanResponder + `window.Arena.dropTreat(type, x, y)` extension. Tap-to-place-at-center still works. (3) Chat noise: removed `promptCompanionReaction` calls from mobile-arena-treat-placed/eaten IPC handlers on the desktop (companion v3.2.16) — they now only log via addDesktopLog, no LLM roundtrip, no chat message. (4) Food preferences: static `COMPANION_FOOD_PREFS` map keyed by companion id/name (substring match) with a default fallback. Preferred treats get a ⭐ star in the picker. (5a) Quest edit diagnostic: added `available: [quest ids]` to the `quests_update_failed` response from desktop (v3.2.16). Mobile toast now shows "wanted X, desktop has [a, b, c]" — diagnostic info for the next test run. (5b) Removed redundant "Active" toggle from quest editor (was confusing with the ⚡ ACTIVE badge). (5c) Goal text editing ships: one TextInput per step with ✕ remove + "+ Add step" + checkbox toggle for completed. Save sends the full goals array, desktop stores as-is. Bumped to v3.10.74 (versionCode 300). Pushed branch + tag to origin. See CHANGES_3.10.74.md for the breakdown and the lessons "debug-before-fix when the obvious cause doesn't pan out" + "cross-system UI consistency needs careful field naming" + "heuristic preferences beat no preferences" + "long-press is a mobile-native gesture that desktop-port patterns miss".

## 2026-07-22 17:31 (channel: #cyber-dev)
- **User:** Tobe reported 5 things from testing the snack + quest edit: (1) "the companion did not move to it" — snacks dropped but no seek behavior; (2) "it should be drag and drop able also" — wants drag-to-place; (3) "the comments the companion makes when given food or played with does not need to appear in the chat" — no AI reaction noise on feed/play; (4) "certain companions should prefer certain foods" — per-companion preferences; (5) quest edit still says "Couldn't update quest: quest not found" even with v3.10.73's gate, plus "it does not need the double active quest or not" (the Status="Active" + ⚡ ACTIVE badge confusion) and "still missing the steps" (goal text editing).
- **You:** v3.10.74 ships all five. (1) Companion-not-moving fix: removed `c.isActive` requirement from seek-and-eat — the v3.10.72 logic only ran for active companions but on the home screen the user can drop a treat before tapping a companion tab (activeId is null at that moment). Desktop doesn't filter by active either, mobile now matches. Added 200px L∞ distance threshold for sanity. (2) Drag-to-place: long-press a treat in the modal → drag overlay follows finger → release places at finger position via PanResponder + `window.Arena.dropTreat(type, x, y)` extension. Tap-to-place-at-center still works. (3) Chat noise: removed `promptCompanionReaction` calls from mobile-arena-treat-placed/eaten IPC handlers on the desktop (companion v3.2.16) — they now only log via addDesktopLog, no LLM roundtrip, no chat message. (4) Food preferences: static `COMPANION_FOOD_PREFS` map keyed by companion id/name (substring match) with a default fallback. Preferred treats get a ⭐ star in the picker. (5a) Quest edit diagnostic: added `available: [quest ids]` to the `quests_update_failed` response from desktop (v3.2.16). Mobile toast now shows "wanted X, desktop has [a, b, c]" — diagnostic info for the next test run. (5b) Removed redundant "Active" toggle from quest editor (was confusing with the ⚡ ACTIVE badge). (5c) Goal text editing ships: one TextInput per step with ✕ remove + "+ Add step" + checkbox toggle for completed. Save sends the full goals array, desktop stores as-is. Bumped to v3.10.74 (versionCode 300). Pushed branch + tag to origin. See CHANGES_3.10.74.md for the breakdown and the lessons "debug-before-fix when the obvious cause doesn't pan out" + "cross-system UI consistency needs careful field naming" + "heuristic preferences beat no preferences" + "long-press is a mobile-native gesture that desktop-port patterns miss".


## 2026-07-22 18:37 (channel: #cyber-dev)
- **User:** Tobe reported (caught on the second ping — the first message returned "Exec failed" from my response stack, so I missed it): "@Clawsuu Okey updated and tested. I think the menu should open inside the arena, and when one item is clicked it should appear as it does now with a ring around it and then the user clicks and holds it to drop it. Now it dropped it in the chat and i cant drag it"
- **You:** v3.10.75 throws out the v3.10.72-v3.10.74 RN-side picket-and-drag dance and puts everything inside the WebView as HTML elements (matching the desktop's #feed-menu pattern). 1. In-arena feed menu (#feed-menu div inside the WebView) opened by tapping the 🍖 button (no RN bridge — pure local toggle). 2. Select-then-drop drag flow: tap a treat in the menu → ring + selected styling, menu closes, ghost emoji follows the finger via touchmove/mousemove. Tap arena canvas → drops at the tap position, calls dropTreat(type, x, y) which uses the existing seek-and-eat, closest companion walks over and eats. Brief "✓ placed" toast for confirmation. 3. Per-companion ⭐ preferences moved from HomeScreen to inside arena.html (uses active companion's id/name substring match). Clawsuu → fish/meat/berry; Lamasuu → cake/cookie/berry; cat/dog/bird get species defaults. 4. Removed ~150 lines of RN-side drag/Modal code (feedModalOpen state, dragMode, placeTreat, dragPanResponder, Modal, ~120 lines of styles, PanResponder import). The RN side just listens for treat_placed/treat_eaten (still works). Tobe's "click a treat then click and hold to drop" was actually a select-then-place pattern (one tap select, one tap place), not a long-press-drag — implemented that simpler flow. Bumped to v3.10.75 (versionCode 301). Pushed branch + tag to origin, CI building. See CHANGES_3.10.75.md and the lessons "WebView-local UI is right for ephemeral controls that don't need RN state" + "pointerEvents='box-none' on the View that owns the PanResponder silently disables the gestures" + "Tobe's UX preference (select-then-place) was clearer than my interpretation (drag-and-place)".

## 2026-07-22 20:01 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu Okey updated and tested. Food is ok now. Tried to edit quest again but still getting the same error. Quest not found, could not update. For some reason" with screenshot showing the v3.10.74 editor open for CYBERHIVE_WEBSITE V2 with 4 steps (1 checked).
- **You:** Companion release v3.10.77 — desktop v3.2.17. Key observation: the toast in Tobe's screenshot did NOT show the v3.10.74 diagnostic info ("wanted X, desktop has [...]"), which tells us his desktop is still on v3.2.15 or earlier. Need to restart the desktop to pick up v3.2.16+ for diagnostics to land. Meanwhile, shipped two improvements: (1) **name-based fallback in `onUpdateQuest`** — if the exact id doesn't match, try to find by name. Single match → recover (log a warning + proceed with update). Multiple matches → refuse to guess. Trades a little safety for usability: a stale id from a desktop restart can be silently corrected when the name matches. (2) **Richer diagnostic info** — failure response now includes full quest list with id+name pairs + `wantedName`. Toast now shows "wanted id X for Y, desktop has: A (id1), B (id2), C (id3)" which makes "stale id for the right quest" vs "id genuinely unknown" obvious. Bumped to v3.10.77 (versionCode 302). Pushed branch + tag to origin. See CHANGES_3.10.77.md for the lessons "when diagnostic info didn't show up, that itself is diagnostic" + "diagnostic info should distinguish wrong target from no target".

## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 17:31 (channel: #cyber-dev)
- **User:** Tobe reported 5 things from testing the snack + quest edit: (1) "the companion did not move to it" — snacks dropped but no seek behavior; (2) "it should be drag and drop able also" — wants drag-to-place; (3) "the comments the companion makes when given food or played with does not need to appear in the chat" — no AI reaction noise on feed/play; (4) "certain companions should prefer certain foods" — per-companion preferences; (5) quest edit still says "Couldn't update quest: quest not found" even with v3.10.73's gate, plus "it does not need the double active quest or not" (the Status="Active" + ⚡ ACTIVE badge confusion) and "still missing the steps" (goal text editing).
- **You:** v3.10.74 ships all five. (1) Companion-not-moving fix: removed `c.isActive` requirement from seek-and-eat — the v3.10.72 logic only ran for active companions but on the home screen the user can drop a treat before tapping a companion tab (activeId is null at that moment). Desktop doesn't filter by active either, mobile now matches. Added 200px L∞ distance threshold for sanity. (2) Drag-to-place: long-press a treat in the modal → drag overlay follows finger → release places at finger position via PanResponder + `window.Arena.dropTreat(type, x, y)` extension. Tap-to-place-at-center still works. (3) Chat noise: removed `promptCompanionReaction` calls from mobile-arena-treat-placed/eaten IPC handlers on the desktop (companion v3.2.16) — they now only log via addDesktopLog, no LLM roundtrip, no chat message. (4) Food preferences: static `COMPANION_FOOD_PREFS` map keyed by companion id/name (substring match) with a default fallback. Preferred treats get a ⭐ star in the picker. (5a) Quest edit diagnostic: added `available: [quest ids]` to the `quests_update_failed` response from desktop (v3.2.16). Mobile toast now shows "wanted X, desktop has [a, b, c]" — diagnostic info for the next test run. (5b) Removed redundant "Active" toggle from quest editor (was confusing with the ⚡ ACTIVE badge). (5c) Goal text editing ships: one TextInput per step with ✕ remove + "+ Add step" + checkbox toggle for completed. Save sends the full goals array, desktop stores as-is. Bumped to v3.10.74 (versionCode 300). Pushed branch + tag to origin. See CHANGES_3.10.74.md for the breakdown and the lessons "debug-before-fix when the obvious cause doesn't pan out" + "cross-system UI consistency needs careful field naming" + "heuristic preferences beat no preferences" + "long-press is a mobile-native gesture that desktop-port patterns miss".

## 2026-07-22 17:31 (channel: #cyber-dev)
- **User:** Tobe reported 5 things from testing the snack + quest edit: (1) "the companion did not move to it" — snacks dropped but no seek behavior; (2) "it should be drag and drop able also" — wants drag-to-place; (3) "the comments the companion makes when given food or played with does not need to appear in the chat" — no AI reaction noise on feed/play; (4) "certain companions should prefer certain foods" — per-companion preferences; (5) quest edit still says "Couldn't update quest: quest not found" even with v3.10.73's gate, plus "it does not need the double active quest or not" (the Status="Active" + ⚡ ACTIVE badge confusion) and "still missing the steps" (goal text editing).
- **You:** v3.10.74 ships all five. (1) Companion-not-moving fix: removed `c.isActive` requirement from seek-and-eat — the v3.10.72 logic only ran for active companions but on the home screen the user can drop a treat before tapping a companion tab (activeId is null at that moment). Desktop doesn't filter by active either, mobile now matches. Added 200px L∞ distance threshold for sanity. (2) Drag-to-place: long-press a treat in the modal → drag overlay follows finger → release places at finger position via PanResponder + `window.Arena.dropTreat(type, x, y)` extension. Tap-to-place-at-center still works. (3) Chat noise: removed `promptCompanionReaction` calls from mobile-arena-treat-placed/eaten IPC handlers on the desktop (companion v3.2.16) — they now only log via addDesktopLog, no LLM roundtrip, no chat message. (4) Food preferences: static `COMPANION_FOOD_PREFS` map keyed by companion id/name (substring match) with a default fallback. Preferred treats get a ⭐ star in the picker. (5a) Quest edit diagnostic: added `available: [quest ids]` to the `quests_update_failed` response from desktop (v3.2.16). Mobile toast now shows "wanted X, desktop has [a, b, c]" — diagnostic info for the next test run. (5b) Removed redundant "Active" toggle from quest editor (was confusing with the ⚡ ACTIVE badge). (5c) Goal text editing ships: one TextInput per step with ✕ remove + "+ Add step" + checkbox toggle for completed. Save sends the full goals array, desktop stores as-is. Bumped to v3.10.74 (versionCode 300). Pushed branch + tag to origin. See CHANGES_3.10.74.md for the breakdown and the lessons "debug-before-fix when the obvious cause doesn't pan out" + "cross-system UI consistency needs careful field naming" + "heuristic preferences beat no preferences" + "long-press is a mobile-native gesture that desktop-port patterns miss".


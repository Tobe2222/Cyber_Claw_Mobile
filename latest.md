## 2026-07-23 13:00 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu good good. I was pondering about adding which quest each text in the chat is related to. Like it says yesterday it could also say which quests was/active when a text is sent in the chat. So when one switches it creates a new headline like yesterday is added above a text. Makes sense?"
- **You:** v3.10.85 ships chat-history-per-quest-labels. Two-part change: (1) Stamp each ChatMessage with activeQuestId/activeQuestName at append time (snapshot-at-append, not at render — so the chat history is self-documenting even if the active quest changes later). All three creation sites stamp: desktop echo, local voice-mode user msg, typed user msg. (2) Track the active quest in HomeScreen via a quests_list listener + ref. renderMessage now compares messages[index-1].activeQuestId to item.activeQuestId. When different (with a careful undefined-vs-null table that handles legacy messages from before v3.10.85), show a thin gold-bordered pill above the current message: "🎯 <QuestName>" or "No active quest". Comparison handles: undefined↔undefined: no separator (both legacy), undefined↔null: no separator (legacy into default), id1↔id2: separator (quest switch), null↔id: separator (entering or leaving a quest). Legacy messages in AsyncStorage cache just render without separators; new messages after the broadcast lands show their quest labels. Bumped to v3.10.85 (versionCode 309), tagged + pushed. See CHANGES_3.10.85.md for breakdown + the lessons "snapshot at append, not at render — recording state at the moment of the event is what makes history self-documenting" + "ref not state for syncClient listener context — listeners are registered once with empty deps, state reads would go stale" + "legacy-data compatibility is its own dimension — undefined vs null maps to we-dont-know vs we-know-its-empty, handle both" + "one source of truth two consumers — both QuestsScreen and HomeScreen subscribe to quests_list independently, desktop is canonical, each screen takes what it needs".
- **User (13:07):** Tobe clarified "to be clear, not each individual text but rather conversations. So if i change quest and write a text it says which quest above that text and all texts after dont need a headline until its changed again."
- **You:** Confirmed v3.10.85 already matches that exactly — the pill marks the transition point (prev.activeQuestId !== curr.activeQuestId), consecutive same-quest messages get no pill. DREW a small ASCII diagram in chat showing: transition pill → block of messages under that quest → transition pill (next change) → block of messages → etc.
- **User (13:27):** Tobe confirmed "@Clawsuu Okey cool do it".

## 2026-07-23 12:40 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu when inside the quest menu the phone back swipe exits the program. It should just go back to the home screen".
- **You:** v3.10.84 ships the BackHandler fix. QuestsScreen is state-driven (App.tsx:567 — setScreen('quests')), no native stack navigation. Without a BackHandler, the Android system back press / gesture-nav swipe bubbles up to the activity and exits the app. Every other modal screen (SettingsScreen:474, CompanionSettingsScreen:765, WakeModeScreen:2342) has a BackHandler — QuestsScreen was the only one missing one. Easy to miss because the visual chevron back button already calls onBack, so anyone using the visible back button sees the right behavior. Fix: added BackHandler.addEventListener with nested-modal priority — confirm dialog first (delete confirmation), then editor modal, then detail modal, then onBack() to return home. Each branch returns true to tell the OS "I handled this" and prevent the activity from exiting. Bumped to v3.10.84 (versionCode 308), tagged + pushed. See CHANGES_3.10.84.md for breakdown + the lessons "every state-driven screen needs a BackHandler — state-driven screens look like normal nav but don't get React Navigation's back-stack for free" + "visual back buttons are not enough — Android users have muscle memory for system back, fix both paths" + "the easy-to-forget code path is the one that bites you — BackHandler does nothing visible so reviewers can't see it missing from a screenshot; check for its presence deliberately (grep for BackHandler across src/screens/)".

## 2026-07-23 12:21 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu I still dont see a set as active button on the right side of the quests here. That star is it perhaps but thats not intuitive. Create a bigger set as active button on the right side of each as i asked for". Screenshot showed the v3.10.82 layout: ☆ on the right side of the Cyber_School card next to ✕, both as small icon-only buttons at the same visual weight.
- **You:** v3.10.83 ships the prominent "Set as active" button. v3.10.82 moved ☆ from left to right but kept it as a small icon-only button that visually competed with ✕ (same size, same weight) — user couldn't pick it up as a primary action. Fix: replace the small icon-only ☆ with a labeled pill button — star icon + "Set active" text, gold border + soft gold background, pill shape (borderRadius: 18). When the quest IS active, the same button becomes green (✓ Active) to confirm the current state at a glance. Tapping the green button deactivates (mirrors the no-quest card behavior). ✏️ stays as a small icon-only button on the left (secondary action), ✕ stays as a small icon-only button on the far right edge (destructive, infrequent). Bumped to v3.10.83 (versionCode 307), tagged + pushed. See CHANGES_3.10.83.md for breakdown + the lessons "visual weight matters more than position — moving ☆ to the right was structural fix but not enough; needed bigger weight" + "icon-only buttons are an anti-pattern for primary actions — save icons for secondary actions" + "color is a state indicator — green=current/correct, gold=action available creates a fast scan path" + "two-stage UX fixes are common — position fix not enough when the issue is weight".

## 2026-07-23 11:28 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu The close and edit button positioning is a bit wonky, create a border for them also. And in the quest menu, add a button/toggle on the right side of each there where one can set as active. Much easier than having to go into the quests. Also add a default/no quest for conversations not related to any of them". Screenshot showed the detail modal with Close on the left edge (no flex: 1) and Edit stretching across; list cards with ☆ on the left next to ✏️ and ✕ on the right.
- **You:** v3.10.82 ships three quest-screen polish fixes. (1) Detail-modal footer: Close now gets flex: 1 (was missing — made it hug left edge), both buttons get visible 1px borders + dark background. (2) List-card action row reordered to ✏️ left, ☆ right, ✕ far right (destructive on edge). (3) NEW "No active quest" card at the top of the list — tap to clear the active quest. The desktop's onSetQuestActive handler already supports null/empty id (sets active: false on all quests); the mobile's setQuestActive(id: string | null) and the sync-server's set_quest_active case both pass through unchanged. Only missing piece was the mobile UI. Card uses dashed border when inactive, solid gold border + ★ when active (matches the active-quest visual on regular cards). Bumped to v3.10.82 (versionCode 306), tagged + pushed. See CHANGES_3.10.82.md for breakdown + the lessons "add a feature requests usually reveal hidden state in the protocol — check if the backend already supports it and the only missing piece is UI exposure" + "visual asymmetry on action buttons is a tiny detail that compounds — when users describe positioning as wonky, look for unequal width/height on sibling elements first" + "default-state UIs matter more than they seem — once a quest was active the user was trapped in it".

## 2026-07-23 10:45 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu now it hides under the top of the keyboard. This should be adaptive or smart so it fits all phones if possible" — screenshot showed the input row's bottom edge clipped under Gboard's suggestion bar.
- **You:** v3.10.81 ships the measurement fix. v3.10.80 used endCoordinates.height, which on Android 15+ edge-to-edge reports the keyboard's content area (~240dp, just the keys) and silently excludes the suggestion bar (~50-60dp on Gboard, ~70-80dp on Samsung Keyboard, 0 on Hacker Keyboard). Fix: compute visualHeight from endCoordinates.screenY instead — `Dimensions.get('screen').height - coords.screenY` gives the true visual height including whatever decoration the keyboard app shows. Works across all keyboard apps because it uses the actual visual top, not a derived value with baked-in assumptions. Bumped to v3.10.81 (versionCode 305), tagged + pushed. See CHANGES_3.10.81.md for breakdown + the lessons "endCoordinates.height is NOT the keyboard's visual height on Android 15+" + "when the user says make it adaptive, the answer is usually use the source of truth rather than a derived value" + "two-stage fixes on cross-platform UI bugs are common — ship the first attempt and refine from screenshots".

## 2026-07-23 09:44 (channel: #cyber-dev)
- **User:** Tobe reported "@Clawsuu Okey so i tried to chat in the app but the input field etc seems stuck on the bottom now. It should be above the keyboard, see picture." Screenshot showed the chat list, then a thin gap, then the keyboard — input row nowhere visible.
- **You:** v3.10.80 ships a manual keyboard-avoidance fix for Android. v3.10.71 disabled KeyboardAvoidingView on Android and relied on windowSoftInputMode="adjustResize" to push the chat input up. That worked on Android <15, but the app's targetSdk is 36 — Android 15+ enforces edge-to-edge and silently DISABLES adjustResize for edge-to-edge apps (window doesn't resize, input stays at the bottom of the unresized window, keyboard covers it). Fix: track keyboard height via Keyboard.addListener('keyboardDidShow', ...) and apply it as paddingBottom on the inputContainer on Android. iOS keeps using KeyboardAvoidingView with behavior='padding' (the iOS-native path doesn't have this problem). Bumped to v3.10.80 (versionCode 304), tagged + pushed. See CHANGES_3.10.80.md for the breakdown and the lessons "don't trust 'the OS handles this' without testing on the target platform version" + "Android 15+ edge-to-edge enforcement is invisible until you trip over it — audit all native-handled behaviors when targetSdk changes" + "paddingBottom on the inputContainer is a self-contained keyboard-avoidance primitive that survives any OS-level window management change".
## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 22:04 (channel: #cyber-dev)
- **User:** Tobe reported two things: (1) chat bug — "i got the notification and the companion tab says new message but it does not appear in the chat before i Click the red notification in the companion tab. It should just appear in the chat." Screenshot showed the chat with various messages but a "1" badge on the Clawsuu tab. (2) "The quest edit seems to work now. But i noticed the steps dont appear in the quest page. Those should be listed there." Confirmed the v3.2.19 sync-server fix worked.
- **You:** v3.10.79 ships both fixes. (1) Chat auto-sync: added `useEffect` that syncs `messages` to `messagesByAgent[activeChatAgentId]` whenever `activeChatAgentId` changes. Root cause: `setMessages` was only called in `appendAgentMessage` and `switchToAgent`, but `activeChatAgentId` could change via the agents-list boot path (line 2361) without going through `switchToAgent` — leaving `messages` pointing at a stale agent's chat. The new effect uses the ref to avoid re-running when messagesByAgent itself changes (avoid loop). (2) Inline step list on quest cards: render the first 3 goal steps inline with `☑/☐` checkbox + strikethrough for completed. Longer lists show "+N more (tap card for full list)" hint. Bumped to v3.10.79 (versionCode 303). Pushed to origin. See CHANGES_3.10.79.md for the breakdown and the lessons "when useState and useRef represent the same data they can drift — sync them with useEffect" + "inline > detail-modal when the data is small — reserve detail modals for content that's actually too big to fit".

## 2026-07-22 22:04 (channel: #cyber-dev)
- **User:** Tobe reported two things: (1) chat bug — "i got the notification and the companion tab says new message but it does not appear in the chat before i Click the red notification in the companion tab. It should just appear in the chat." Screenshot showed the chat with various messages but a "1" badge on the Clawsuu tab. (2) "The quest edit seems to work now. But i noticed the steps dont appear in the quest page. Those should be listed there." Confirmed the v3.2.19 sync-server fix worked.
- **You:** v3.10.79 ships both fixes. (1) Chat auto-sync: added `useEffect` that syncs `messages` to `messagesByAgent[activeChatAgentId]` whenever `activeChatAgentId` changes. Root cause: `setMessages` was only called in `appendAgentMessage` and `switchToAgent`, but `activeChatAgentId` could change via the agents-list boot path (line 2361) without going through `switchToAgent` — leaving `messages` pointing at a stale agent's chat. The new effect uses the ref to avoid re-running when messagesByAgent itself changes (avoid loop). (2) Inline step list on quest cards: render the first 3 goal steps inline with `☑/☐` checkbox + strikethrough for completed. Longer lists show "+N more (tap card for full list)" hint. Bumped to v3.10.79 (versionCode 303). Pushed to origin. See CHANGES_3.10.79.md for the breakdown and the lessons "when useState and useRef represent the same data they can drift — sync them with useEffect" + "inline > detail-modal when the data is small — reserve detail modals for content that's actually too big to fit".


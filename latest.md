## 2026-07-19 21:23 (channel: #cyber-dev)
- **User:** Tobe said "go ahead" — proceed with v3.10.64 and clean up broken tags.
- **You:** v3.10.64 ships continuous learning + strict mode toggle. Continuous learning: every confirmed wake blends the recent embeddings into the locked profile (small 0.05 ratio) so it adapts to gradual voice changes. Strict mode: UI toggle in the ActiveEnrollmentPanel that drops Vosk from BG when profile is locked, saving ~10% CPU + ~50MB RAM. Also deleted the broken tags (v3.10.60, v3.10.61, v3.10.62) from origin via `git push origin --delete`. Bumped to v3.10.64 (versionCode 291). The full personalized wake stack is now complete: trainer + bootstrap enrollment + OWW-primary in BG + active enrollment + continuous learning + strict mode. See CHANGES_3.10.64.md for the breakdown and the lesson "for personalized systems, bias toward slow adaptation — small blend ratios feel natural, large ones feel jumpy".
## 2026-07-22 18:37 (channel: #cyber-dev)
- **User:** Tobe reported (caught on the second ping — the first message returned "Exec failed" from my response stack, so I missed it): "@Clawsuu Okey updated and tested. I think the menu should open inside the arena, and when one item is clicked it should appear as it does now with a ring around it and then the user clicks and holds it to drop it. Now it dropped it in the chat and i cant drag it"
- **You:** v3.10.75 throws out the v3.10.72-v3.10.74 RN-side picket-and-drag dance and puts everything inside the WebView as HTML elements (matching the desktop's #feed-menu pattern). 1. In-arena feed menu (#feed-menu div inside the WebView) opened by tapping the 🍖 button (no RN bridge — pure local toggle). 2. Select-then-drop drag flow: tap a treat in the menu → ring + selected styling, menu closes, ghost emoji follows the finger via touchmove/mousemove. Tap arena canvas → drops at the tap position, calls dropTreat(type, x, y) which uses the existing seek-and-eat, closest companion walks over and eats. Brief "✓ placed" toast for confirmation. 3. Per-companion ⭐ preferences moved from HomeScreen to inside arena.html (uses active companion's id/name substring match). Clawsuu → fish/meat/berry; Lamasuu → cake/cookie/berry; cat/dog/bird get species defaults. 4. Removed ~150 lines of RN-side drag/Modal code (feedModalOpen state, dragMode, placeTreat, dragPanResponder, Modal, ~120 lines of styles, PanResponder import). The RN side just listens for treat_placed/treat_eaten (still works). Tobe's "click a treat then click and hold to drop" was actually a select-then-place pattern (one tap select, one tap place), not a long-press-drag — implemented that simpler flow. Bumped to v3.10.75 (versionCode 301). Pushed branch + tag to origin, CI building. See CHANGES_3.10.75.md and the lessons "WebView-local UI is right for ephemeral controls that don't need RN state" + "pointerEvents='box-none' on the View that owns the PanResponder silently disables the gestures" + "Tobe's UX preference (select-then-place) was clearer than my interpretation (drag-and-place)".

## 2026-07-22 18:37 (channel: #cyber-dev)
- **User:** Tobe reported (caught on the second ping — the first message returned "Exec failed" from my response stack, so I missed it): "@Clawsuu Okey updated and tested. I think the menu should open inside the arena, and when one item is clicked it should appear as it does now with a ring around it and then the user clicks and holds it to drop it. Now it dropped it in the chat and i cant drag it"
- **You:** v3.10.75 throws out the v3.10.72-v3.10.74 RN-side picket-and-drag dance and puts everything inside the WebView as HTML elements (matching the desktop's #feed-menu pattern). 1. In-arena feed menu (#feed-menu div inside the WebView) opened by tapping the 🍖 button (no RN bridge — pure local toggle). 2. Select-then-drop drag flow: tap a treat in the menu → ring + selected styling, menu closes, ghost emoji follows the finger via touchmove/mousemove. Tap arena canvas → drops at the tap position, calls dropTreat(type, x, y) which uses the existing seek-and-eat, closest companion walks over and eats. Brief "✓ placed" toast for confirmation. 3. Per-companion ⭐ preferences moved from HomeScreen to inside arena.html (uses active companion's id/name substring match). Clawsuu → fish/meat/berry; Lamasuu → cake/cookie/berry; cat/dog/bird get species defaults. 4. Removed ~150 lines of RN-side drag/Modal code (feedModalOpen state, dragMode, placeTreat, dragPanResponder, Modal, ~120 lines of styles, PanResponder import). The RN side just listens for treat_placed/treat_eaten (still works). Tobe's "click a treat then click and hold to drop" was actually a select-then-place pattern (one tap select, one tap place), not a long-press-drag — implemented that simpler flow. Bumped to v3.10.75 (versionCode 301). Pushed branch + tag to origin, CI building. See CHANGES_3.10.75.md and the lessons "WebView-local UI is right for ephemeral controls that don't need RN state" + "pointerEvents='box-none' on the View that owns the PanResponder silently disables the gestures" + "Tobe's UX preference (select-then-place) was clearer than my interpretation (drag-and-place)".


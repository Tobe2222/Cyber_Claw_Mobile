## 2026-06-14 18:35 (channel: #cyber-dev)
- **User:** Acknowledged v3.1.6/v3.1.7 desktop fixes ("good work little goblin, here have a cracker!"). Asked to make the mobile extension compatible with the latest desktop. Specifically: remove the mobile Arena Settings screen (the bg/companion picker is redundant with desktop; mobile should be a true extension). Verify the rest is compatible. Tag and push a new version. Also re-check the wake word → home screen bug — last time it was still opening the home screen instead of going to Wake Mode.
- **You:** v3.1.12 mobile: (1) Removed the entire ArenaSettingsScreen (bg+companion+voice). The voice TTS settings stay in the regular Settings screen; only the redundant bg+companion picker is gone. (2) Fixed the wake word bug by moving the wake UI to a dedicated `WakeModeScreen` route. App.tsx listens for the wake event at the top level (NativeModule emitter, DeviceEventEmitter, AsyncStorage fallback) and switches to WakeModeScreen. There is no longer any way to land on the home screen because wake mode is a different screen. HomeScreen no longer listens for wake events. Manual Wake Mode button + test button + arena button all go to the same WakeModeScreen. (3) Verified companion IDs and bg IDs match the desktop catalog. (4) `tsc --noEmit` clean for new code (one pre-existing error in HomeScreen.tsx line 1699 unrelated). (5) Bumped to v3.1.12 (versionCode 62) and tagged via bump.sh. Also pushed desktop v3.1.7 (the spirit cleanup + _buildCompanion fix) to GitHub.

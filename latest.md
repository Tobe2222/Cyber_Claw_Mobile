## 2026-06-14 19:15 (channel: #cyber-dev)
- **User:** Acknowledged v3.1.12 ("very nice work"). Noticed that the voice settings used to live in the deleted ArenaSettingsScreen and asked to put them in the general settings, also clean up and categorize the settings properly, and make sure it's a good UX.
- **You:** v3.1.13 mobile: (1) Restored the lost voice settings as a new "🔊 Voice & Speech" section — local TTS toggle, voice selection (default/male/female), test local voice button, test desktop voice button, premium API section (clearly marked "coming soon" since the desktop doesn't consume it yet). (2) Reorganized the previous 8+ flat sections into 5 well-defined categories: Connection, Permissions, Wake Word, Voice & Speech, Agent Reach. All wake-related stuff in one place, all permissions in one place. (3) Removed 7 dead useState declarations (ttsEnabled, wakePhrase, wakeMode, ppnPath, wakeTrained, showTrainer, showTrainingManager). (4) UX cleanups: wake greeting input no longer has a redundant Save button (just one TextInput with debounced auto-save); the bottom "Save Settings" button is gone (only the audio buffer settings need explicit save, and that's now a small "Save audio settings" button inside the Wake Word section); wake permissions moved from Always Listening into the main Permissions section; new About footer with version + GitHub link. (5) File is 831 lines (down from 1114) despite adding a whole new section, thanks to reusable Section/Toggle/OptionBtn inline components. (6) All AsyncStorage keys preserved, no data migration. tsc clean. Bumped to v3.1.13 (versionCode 63) and tagged.

## 2026-07-14 10:08 (channel: #cyber-dev)
- **User:** Tobe reported that v3.10.3 still showed "no wake yet" on the per-companion "Wake settings" card (with the misleading "uses default wake if no active wake is bound" text), the Wake sub-page picker was empty, and the wake word was triggering falsely. Wake Sets manager correctly showed "Hey Clawsuu" as ✓ Active.
- **You:** v3.10.4 (commit 1307931, tag v3.10.4): (1) Merged the JS-side `getSavedWakeModels` (active-only, has absolute .tflite path) with `listWakeSets` (all sets, includes `active: bool` flag) as a fallback. The merged fetch uses the manager's source of truth as the universal fallback — both screens now see the same data the manager does. Applied to `CompanionSettingsScreen.savedWakeModels`, `SettingsScreen.savedWakeModels`, and `WakePhrasePicker`'s internal `localSavedModels`. (2) Replaced the misleading "Not trained — uses default wake if no active wake is bound" text with "No active wake on this phone — open Wake Sets to manage trained phrases" — neutral and points the user at the manager. (3) Raised the BG + foreground PhoneticMatcher threshold from 0.55 to 0.7 (single-word "hey" partial now scores 0.57, which is below the threshold). Plus a token-count guard requiring `>= N-1` heard words for a multi-word target. (4) `startBgService` now falls through `listWakeSets` + `getActiveWakeSet(companionId)` when `cyberclaw-audio-settings.wakeWord` is empty/stale, instead of falling back to the literal `'hey clawsuu'` default. Pushed v3.10.4 (versionCode 231). See CHANGES_3.10.4.md for the full breakdown and the rule "when two screens read the same data via different paths and disagree, the higher-trust view wins — don't paper over with more refetch logic".

## 2026-07-14 11:26 (channel: #cyber-dev)
- **User:** Tobe reported that v3.10.4 still says "No active wake on this phone" on the Wake settings card for Clawsuu even though the Wake Sets manager (separate screen) shows "Hey Clawsuu" as ✓ Active. The text under "Wake phrases" on the Wake sub-page is stale ("Tap 🎙 to retrain, 🗑 to delete") and refers to UI that no longer lives on that page. Tobe also asked for the Wake settings page to show the active wake phrase / file directly so they don't have to tap "Manage" to see it.
- **You:** v3.10.5: (1) Added a canonical `activeWakeDirect` state that calls `getActiveWakeSet(companion.id)` + `listWakeSets()` — the same calls the manager uses — and keeps the result in a SEPARATE state slot from `savedWakeModels` so the canonical truth can't be masked by the merge. The wake card's status line prefers this lookup; the merge remains as a fallback for companions that have sets but no active binding. (2) Added a "Currently active wake" panel on the Wake sub-page: green ◉ indicator + phrase (large) + setId (monospace, green) + .tflite path (monospace, grey) + a "📂 Manage wake sets" button. When no wake is active, a dashed-grey empty-state panel points at the Train / Manage buttons. (3) Updated the stale "Wake phrases" hint to "Trained wake phrases for X. Use 'Manage wake sets' below to retrain, rename, or delete." — matches the actual UI (no inline retrain/delete buttons on this page anymore). v3.10.5 (versionCode 232). See CHANGES_3.10.5.md for the breakdown and the lesson "when two screens disagree, ask the canonical question directly and accept the duplicate fetch as the price of correctness".

## 2026-07-14 12:42 (channel: #cyber-dev)
- **User:** Tobe acknowledged the v3.10.5 wake-card fix works ("Okey cool. Lets continue"). Asked to: (1) restart the cyberclaw desktop (mobile showed disconnected — the desktop's electron process had crashed at 12:36 with a GPU fatal error), (2) move Send word + Your-turn cue sound into a new top-level "Voice mode" section, separate from Companions (they're universal settings, not per-companion), (3) clean up the active wake panel on the Wake sub-page — it was showing "Hey Clawsuu" twice (once big in white for displayName, once small in green for setId — both equal because Tobe had renamed the set to "Hey Clawsuu") and had a redundant "Manage wake sets" button (one already exists further down the page).
- **You:** (1) Restarted the cyberclaw desktop (electron process was crashed with GPU fatal error — `nohup setsid npm start` from `/media/humpsuu/CYBERDRIVE/2B/work/projects/cyberclaw`; now broadcasting agents_list on ws://0.0.0.0:9247). (2) v3.10.6: created new top-level `<Section title="🎙️ Voice mode">` in `SettingsScreen.tsx` containing the Send word + Your-turn cue blocks, removed them from the Companions Section. (3) v3.10.6: hidden the setId line in the active-wake panel when it equals the displayName/phrase; removed the redundant "Manage wake sets" button from the panel (and its now-unused `activeWakeManageBtn*` styles). v3.10.6 (versionCode 233). See CHANGES_3.10.6.md for the breakdown and the lessons "visual grouping implies semantic grouping" + "when two fields are perfectly correlated, hide the redundant one".
